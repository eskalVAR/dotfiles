#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return
alias vim='nvim'
alias ls='ls --color=auto'
alias la='ls -al --color=auto'
PS1='[\u@\h \W]\$ '
# Load python Virutal env
# source /usr/bin/virtualenvwrapper.sh

